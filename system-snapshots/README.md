# System Snapshots

Only redacted snapshots belong here.

Before publishing:

```bash
./scripts/audit-redaction.sh system-snapshots
```

Do not publish raw dumps, full package lists, serial numbers, machine IDs, MAC addresses, IP addresses, emails, tokens, WiFi SSIDs, or private hostnames.
